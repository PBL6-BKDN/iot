# direnv configuration for UV
# Install direnv: sudo apt-get install direnv
# Then: echo 'eval "$(direnv hook bash)"' >> ~/.bashrc

export PATH="$HOME/.local/bin:$PATH"

# Auto-activate UV virtual environment
if [ -d .venv ]; then
    source .venv/bin/activate
fi

